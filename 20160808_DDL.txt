
/* EMP01 TABLE CREATE*/
CREATE TABLE EMP01(
    EMPNO NUMBER(4),
    ENAME VARCHAR2(20),
    SAL NUMBER(7,2)
);

CREATE TABLE DEPT01(
    DEPTNO NUMBER(2),
    DNAME VARCHAR2(14),
    LOC VARCHAR2(13)
);

DESC DEPT01


/* 
     
Sub Query를 이용한 테이블 생성 CAS생성
    CAS : 데이터만 복사 , 구조는 복사 되지않음 (EX.primary key)
*/
CREATE TABLE EMP02 
AS 
SELECT * FROM EMP;

DESC EMP02;

SELECT * FROM EMP02;


/*EMPNO,ENAME - COPY*/
CREATE TABLE EMP03
AS
SELECT EMPNO,ENAME FROM EMP;

SELECT EMPNO,ENAME FROM EMP;

SELECT * FROM EMP03;



/*EMPNO, ENAME, SAL - COPY*/
CREATE TABLE EMP04
AS
SELECT EMPNO,ENAME,SAL
FROM EMP;

SELECT * FROM  EMP04;


/*DEPTNO=10 - COPY*/
CREATE TABLE EMP05
AS
SELECT * FROM EMP
WHERE DEPTNO=10;

DESC EMP05

SELECT * FROM EMP05


/*TABLE STRUCT COPY*/
CREATE TABLE EMP06
AS
SELECT * FROM EMP
WHERE 1 = 0;

SELECT * FROM EMP06;


CREATE TABLE DEPT02
AS
SELECT * FROM DEPT
WHERE 1=0;

SELECT * FROM DEPT02;



/*ALTER TABLE*/
DESC EMP01;



/*EMP01 TABLE ADD JOB COLUMN*/
ALTER TABLE EMP01
ADD (JOB VARCHAR2(9));

DESC EMP

/*DEPT02 TABLE ADD DMGR COLUMN*/
ALTER TABLE DEPT02
ADD (DMGR VARCHAR2(10));

DESC DEPT02;


/*EMP01 TABLE MODIFY JOB*/
ALTER TABLE EMP01
MODIFY(JOB VARCHAR2(30));

DESC EMP01;



ALTER TABLE DEPT02
MODIFY(DMGR NUMBER(4));



/* EMP01 테이블 JOB 컬럼 삭제*/
ALTER TABLE EMP01
DROP COLUMN JOB;

DESC EMP01;

ALTER TABLE DEPT02
DROP COLUMN DMGR;

DESC DEPT02;


/*EMP01 TABLE 삭제*/
DROP TABLE EMP01;

/*전체 TABLE 조회*/
SELECT * FROM TAB;

/*데이터 딕셔너리 뷰*/
DESC USER_TABLES;

SELECT* FROM USER_TABLES;

/* 테이블 구조 확인 */
SELECT TABLE_NAME
FROM USER_TABLES
ORDER BY TABLE_NAME DESC;

/* ALL_ 데이터 딕셔너리*/
DESC ALL_TABLES;

SELECT * FROM ALL_TABLES;